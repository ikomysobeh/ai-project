services:
  # --- Laravel bridge/API ---
  app:
    build:
      context: ./ai-bridge
      dockerfile: Dockerfile
    container_name: tokenforge_app
    restart: unless-stopped
    env_file:
      - ./ai-bridge/.env
    environment:
      RUN_MIGRATIONS: "true"
    volumes:
      - ./ai-bridge:/var/www/html
      - ai_bridge_vendor:/var/www/html/vendor
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks: [tokenforge]

  nginx:
    image: nginx:alpine
    container_name: tokenforge_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./ai-bridge:/var/www/html:ro
      - ./ai-bridge/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks: [tokenforge]

  # Vite needs PHP too (the wayfinder plugin shells out to `php artisan
  # wayfinder:generate`), so it reuses the app image instead of a plain
  # Node image.
  vite:
    build:
      context: ./ai-bridge
      dockerfile: Dockerfile
    container_name: tokenforge_vite
    restart: unless-stopped
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
    env_file:
      - ./ai-bridge/.env
    environment:
      RUN_MIGRATIONS: "false"
      RUN_NPM_INSTALL: "true"
    ports:
      - "5173:5173"
    volumes:
      - ./ai-bridge:/var/www/html
      - ai_bridge_vendor:/var/www/html/vendor
      - ai_bridge_node_modules:/var/www/html/node_modules
    depends_on:
      app:
        condition: service_started
    networks: [tokenforge]

  queue:
    build:
      context: ./ai-bridge
      dockerfile: Dockerfile
    container_name: tokenforge_queue
    restart: unless-stopped
    command: ["php", "artisan", "queue:work", "--tries=3", "--backoff=5"]
    env_file:
      - ./ai-bridge/.env
    environment:
      RUN_MIGRATIONS: "false"
    volumes:
      - ./ai-bridge:/var/www/html
      - ai_bridge_vendor:/var/www/html/vendor
    depends_on:
      app:
        condition: service_started
    networks: [tokenforge]

  # --- Data stores ---
  postgres:
    image: pgvector/pgvector:pg16
    container_name: tokenforge_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-tokenforge}
      POSTGRES_USER: ${POSTGRES_USER:-tokenforge}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tokenforge}
    ports:
      - "5432:5432" # comment out once you don't need host DB access for debugging
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tokenforge}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks: [tokenforge]

  redis:
    image: redis:7-alpine
    container_name: tokenforge_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks: [tokenforge]

  # --- Embeddings (RAG) ---
  ollama:
    image: ollama/ollama:latest
    container_name: tokenforge_ollama
    restart: unless-stopped
    ports:
      - "11434:11434" # comment out once you don't need host access for debugging
    volumes:
      - ollama_data:/root/.ollama
    networks: [tokenforge]

  # One-shot: pulls the embedding model on first boot, then exits.
  ollama-init:
    image: ollama/ollama:latest
    container_name: tokenforge_ollama_init
    depends_on:
      - ollama
    environment:
      OLLAMA_HOST: http://ollama:11434
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >
        until ollama list >/dev/null 2>&1; do echo "waiting for ollama..."; sleep 2; done;
        ollama pull nomic-embed-text
    networks: [tokenforge]

  # --- Gemini bridge (internal only, not published to the internet) ---
  webai:
    build:
      context: ./WebAI-to-API
    container_name: tokenforge_webai
    restart: unless-stopped
    env_file:
      - ./WebAI-to-API/.env
    environment:
      PYTHONPATH: /app/src
      PLAYWRIGHT_HEADLESS: "true"
    ports:
      - "127.0.0.1:6969:6969" # localhost-only, for local debugging; remove for real deployments
    volumes:
      - ./WebAI-to-API/config.conf:/app/config.conf:ro
      - ./WebAI-to-API/runtime:/app/runtime
    networks: [tokenforge]

networks:
  tokenforge:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  ollama_data:
  ai_bridge_vendor:
  ai_bridge_node_modules:
